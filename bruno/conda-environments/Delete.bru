meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/conda-environments/{{condaEnvUUID}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ourJWT}}
}

tests {
  test("Status code is 204", function () {
      expect(res.getStatus()).to.equal(204);
  });
}
